const modKey: Phoenix.ModifierKey[] = ['ctrl', 'option', 'shift'];
const modKeyShift: Phoenix.ModifierKey[] = [...modKey, 'cmd'];
const focusOnMouseMove = true;

export {
  modKey,
  modKeyShift,
  focusOnMouseMove,
}
